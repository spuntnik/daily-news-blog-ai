import type { SiteProfile, TrendSignal } from "./types";

export async function fetchGoogleTrendSignals(profile: SiteProfile): Promise<TrendSignal[]> {
  // Phase 2 stub.
  // Wire a real provider next:
  // - unofficial Google Trends package
  // - SerpAPI
  // - DataForSEO
  // - other trend provider

  return [];
}
